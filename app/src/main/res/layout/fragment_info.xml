<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/fragment_statistics"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/main_bg"
    android:paddingBottom="24dp">

    <!-- Общий вертикальный контейнер - единственный ребенок ScrollView -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="35dp"
        android:paddingEnd="35dp">

        <!-- ВЕРХНЯЯ ПАНЕЛЬ: горизонтальный LinearLayout -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="35dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/homeButton"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:background="@android:color/transparent"
                android:scaleType="fitXY"
                android:src="@drawable/home_button" />

            <ImageView
                android:id="@+id/gameTitleImage"
                android:layout_width="0dp"
                android:layout_height="64dp"
                android:layout_weight="1"
                android:scaleType="fitCenter"
                android:src="@drawable/statistic_button" />

            <View
                android:layout_width="64dp"
                android:layout_height="64dp" />
        </LinearLayout>

        <!-- КОНТЕЙНЕР ДЛЯ СПИСКА -->
        <LinearLayout
            android:id="@+id/gameResultsContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="32dp"
            android:orientation="vertical" />

    </LinearLayout>
</ScrollView>
